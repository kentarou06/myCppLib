#include <iostream>
#include <vector>
#include <complex>
#include "fft.h"
using namespace std;

namespace speech{

  template <typename T>
  bool dft<T>::cdft(vector< complex<T> > x, const bool isIDFT ){
    return false;
  }

};
